@page "/projects"
@using ProjectBank.Infrastructure.Generator;

<PageTitle>Projects</PageTitle>

<h1>My Projects</h1>

Insert text here:
<div class = "tags">    
    <button class="btn btn-primary" >Generate</button>
    <br/>
    <br/>
    @foreach (String item in getTags(s))
    {
        <input type="checkbox"/>
        <span>@item</span>
        <br/>
    }
</div>

@code{

    public string text {get; set;}
    public List<String> list {get; set;}
    String s = "Hej dette er min text som jeg prøver at skrive";
    public IEnumerable<String> getTags(String text){
        TagGenerator tagGenerator = new TagGenerator();
        List<String> tags = new List<string>{"hej", "at", "Skriver", "det", "er"};
        tagGenerator.Add(tags);
        tagGenerator.Build();
        return tagGenerator.Find(text);
    }
}

TODO: Implement
